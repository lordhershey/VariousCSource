
This routine take an XML document as a string, you provide a tag and the routine will gave you the text that the tag encloses. It is very useful for parsing out very simple XML.

Improvments possible:
multiple tags returned as multi-dimensional array.
Given a tag, ignore name space portion.
given a tag - return an array of tag attributes.

